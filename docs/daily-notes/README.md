# Daily notes

History (what happened, which commit, when) lives here — `CLAUDE.md` stays
about what's *true now* and *why*, not a changelog. Same split, same
reasoning, as the web repo (`nahunp/todo-app`).

One file per day, `YYYY-MM-DD.md`, `Done today` / `Plan for tomorrow` /
`Blockers` shape — see `TEMPLATE.md`. No `backend/`/`frontend/` split the
way the web repo has: this repo has one owner (Claude, per Diego's
"do it yourself" instruction — see the web repo's `CLAUDE.md`), so there's
no second side's log to keep separate.
