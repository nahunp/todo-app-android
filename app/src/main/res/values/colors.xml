<resources>
    <!-- Used by the adaptive launcher icon background only — everything
         else in the app gets its color from presentation/theme/Color.kt
         (Compose), not from XML resources. -->
    <color name="ic_launcher_background">#F1EFE8</color>
</resources>
